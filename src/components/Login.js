import { useLocation } from "react-router-dom";

const Login = () => {

    //let location = useLocation();

    return (
      <div>Login Page </div>
    //   {location.state.msg}
    )
  }
  
  export default Login;